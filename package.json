{
  "name": "apuestas-pro-static",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check providers/betcris.mjs",
    "static": "python3 -m http.server 3000",
    "install:browsers": "npx playwright install chromium"
  },
  "optionalDependencies": {
    "playwright": "^1.56.1"
  }
}
